version: '3.0'

services:
  main:
    platform: linux/arm64
    container_name: main
    build:
      context: .
    env_file:
      - .env
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - 7000:7000
    command: npm run start:dev
